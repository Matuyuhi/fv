wrap — VIEW レーン: 折返し表示 (w) — 続き行の gutter pad (64x18)
┌────────────────────────────────────────────────────────────────┐
┌fv-preview-fixtur┐┌src/main.rs────────────────────────────────┐
│▸ assets         ││ 1 mod app;                                │
│▸ docs           ││ 2 mod ui;                                 │
│▾ src            ││ 3                                         │
│  ▸ app          ││ 4 use std::error::Error;                  │
│     M main.rs   ││ 5 use std::path::PathBuf;                 │
│    ?? new_lane.r││ 6                                         │
│    M  ui.rs     ││ 7 use app::App;                           │
│  README.md      ││ 8                                         │
│                 ││ 9 fn main() -> Result<(), Box<dyn Error>> │
│                 ││   {                                       │
│                 ││10     let root = std::env::args().nth(1).u│
│                 ││   nwrap_or_else(|| ".".to_string());      │
│                 ││11     let mut app = App::new("sample-app")│
│                 ││   ;                                       │
│                 ││12▎    let mode = Lane::Git;               │
└─────────────────┘└───────────────────────────────────────────┘
[VIEW] EDIT  GIT  LOG   main  j/k: scroll  w: wrap  /: search  e
└────────────────────────────────────────────────────────────────┘
wrap — セルのスタイル (18 種)
┌────────────────────────────────────────────────────────────────┐
aaaaaaaaaaaaaaaaaaasssssssssssssssssssssssssssssssssssssssssssss
aLLLLLLLLLLLLLLLLLas333???CCCCCTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTs
arrrrrrrrrrrrrrrrras333???CCCCTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTs
arrrrrrrrrrrrrrrrras333TTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTs
aLLLLLLLLLLLLLLLLLas333???CCCCCCCCCCCCCCCCCCCTTTTTTTTTTTTTTTTTTs
ahhhhhhhhhhhhhhhhhas333???CCCCCCCCCCCCCCCCCCCCTTTTTTTTTTTTTTTTTs
aVVVVVVVVVVVVVVVVVas333TTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTs
aAAAAAAAAAAAAAAAAAas333???CCCCCCCCCCTTTTTTTTTTTTTTTTTTTTTTTTTTTs
a.................as333TTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTs
a.................as333??C&&&&CCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCs
a.................asTTTCTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTs
a.................as333CCCC???CCCCCCCCCCCCCCCCCCCCCCCCCSSSCOCCSs
a.................asTTTSSSSSSSSSSSSSCCCCCQCCSSSSSSSSSCCCCTTTTTTs
a.................as333CCCC???C???CCCCCCCCCCCCCCCCCQQQQQQQQQQCCs
a.................asTTTCTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTs
a.................assssCCCC???CCCCCCCCCCCCCCCCCCTTTTTTTTTTTTTTTs
aaaaaaaaaaaaaaaaaaasssssssssssssssssssssssssssssssssssssssssssss
qqqqqqddddddddddddddddddbbbbdddddddddddddddddddddddddddddddddddd
└────────────────────────────────────────────────────────────────┘
  T  bg=#2b303b
  &  fg=#8fa1b3 bg=#2b303b
  S  fg=#96b5b4 bg=#2b303b
  Q  fg=#a3be8c bg=#2b303b
  ?  fg=#b48ead bg=#2b303b
  C  fg=#c0c5ce bg=#2b303b
  O  fg=#d08770 bg=#2b303b
  L  fg=blue
  s  fg=cyan bg=#2b303b
  a  fg=darkgray
  3  fg=darkgray bg=#2b303b
  V  fg=green dim
  b  fg=magenta bg=darkgray
  d  fg=white bg=darkgray
  q  fg=white bg=darkgray bold reversed
  r  fg=yellow
  h  fg=yellow bg=darkgray bold dim
  A  fg=yellow dim
