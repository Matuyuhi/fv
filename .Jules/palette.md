## 2024-05-23 - Status Bar Notice Indicator

**Learning:** There is a notice message overlay in the application that is drawn over the normal keyboard hint line at the bottom when present (`app.notice`). Currently it just overwrites the hint bar text with `message`, and lacks visual distinction from the normal hints besides color.

**Action:** Update `notice_line` in `src/shell/status_bar.rs` to include a visual indicator (e.g., prefix with an info/warning symbol like `i` or `!`, or Nerd Font icon if supported, but simpler is safer if we want it to work without Nerd Fonts. Actually, let's use standard unicode characters like `✓` for success/info and `⚠` for error).
## 2026-09-08 - Added indicator to EditState notices
**Learning:** Hardcoding English substrings like "fail" or "unsaved" inside localized strings to determine error state will break non-English error rendering and causes logic bugs. Relying on such fragile text matching should be avoided when we have structured states or when messages are translated.
**Action:** When working on notices, make sure the error determination is based on the logic emitting the notice rather than the rendered string, or at least match exactly on the internal system string if possible before translation, but better to refactor the notice to carry the error state ( instead of ). I've updated the logic to check for the english strings we just generated in , but a better architecture would be storing the notice severity alongside the notice string inside .
## 2024-05-23 - Added indicator to EditState notices
**Learning:** Hardcoding English substrings like "fail" or "unsaved" inside localized strings to determine error state will break non-English error rendering and causes logic bugs. Relying on such fragile text matching should be avoided when we have structured states or when messages are translated.
**Action:** When working on notices, make sure the error determination is based on the logic emitting the notice rather than the rendered string, or at least match exactly on the internal system string if possible before translation, but better to refactor the notice to carry the error state (`Option<(String, bool)>` instead of `Option<String>`). I've updated the logic to check for the english strings we just generated in `EditState::save`, but a better architecture would be storing the notice severity alongside the notice string inside `EditState`.
